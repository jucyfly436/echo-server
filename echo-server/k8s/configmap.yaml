apiVersion: v1
kind: ConfigMap
metadata:
  name: echo-server-config
data:
  REDIS_HOST: "127.0.0.1"  # 因为在同一个 Pod 内，通过 localhost 即可访问
  REDIS_PORT: "6379"
  REDIS_PASSWORD: ""  #  Redis 没有设置密码，留空